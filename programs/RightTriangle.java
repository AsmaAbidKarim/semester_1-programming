public class RightTriangle
{
	public static void main(String[] args)
	{	
		int a=Math.random();
		int b=Math.random();
		int c=Math.random();
		
		int z=(Math.max(a, Math.max(b, c)));
		int totalsum=((a+b+c)-z);
		if(totalsum>z)
		{
			Sy	
		}
	}
}